defaults:
  - noise: two_timescale
  - _self_ # inherit global defaults

noise:
  time_units: years
  output:
    format: netcdf

paths:
  project_root: "/Users/ericm/Documents/Research/methane_noise_forcing"
  data_dir: "data"
  configs_dir: "configs" 
  figures_dir: "figures"
  
  # Derived paths (relative to project_root)
  noise_realizations_root: "${paths.data_dir}/noise_realizations"
  noise_realizations_raw: "${paths.noise_realizations_root}/raw"
  noise_realizations_smoothed: "${paths.noise_realizations_root}/smoothed"
  noise_realizations_sampled: "${paths.noise_realizations_root}/sampled"

  observations_root: "${paths.data_dir}/observations"
  observations_cultivated: "${paths.observations_root}/cultivated"